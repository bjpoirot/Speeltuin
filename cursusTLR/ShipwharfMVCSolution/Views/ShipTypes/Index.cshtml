@model PagedViewModel<ShipTypePageListItemViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Items.FirstOrDefault().Title)
            </th>
            <th>
                Count
            </th>
            <th>

            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var shiptype in Model.Items)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => shiptype.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => shiptype.Count)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@shiptype.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@shiptype.Id" asp-route-aapje="@shiptype.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@shiptype.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<partial name="_Pager" model="@Model" />
